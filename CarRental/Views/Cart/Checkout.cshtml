@model CarRental.ViewModels.CartVM
@{
    ViewData["Title"] = "Checkout";
    Layout = "~/Views/Shared/_Layout.cshtml";
    List<CarRentalOrder> carRentalOrders = ViewBag.Order;
}

<!-- Breadcrumb Area Start Here -->
<div class="breadcrumbs-area position-relative">
    <div class="container">
        <div class="row">
            <div class="col-12 text-center">
                <div class="breadcrumb-content position-relative section-content">
                    <h3 class="title-3">Thanh toán</h3>
                    <ul>
                        <li><a href="index.html">Trang chủ</a></li>
                        <li>Thanh toán</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Breadcrumb Area End Here -->
<!-- Checkout Area Start Here -->
<div class="checkout-area mt-no-text" style="margin-bottom:500px">
    <div class="container custom-container">
        <div class="row">
            <div class="col-12 col-custom">
                <div class="coupon-accordion" style="background-color:#FEF7F4; ">
                    <h3>Giấy tờ cho thuê xe? <span id="showlogin">Tìm hiểu thêm</span></h3>
                    <div id="checkout-login" class="coupon-content" style="background-color:#FEF7F4; border-left: 8px solid #f26a2b;">
                        <div class="coupon-info">
                            <p class="coupon-text">
                                <i class="bi bi-exclamation-circle-fill"></i> chọn 1 trong hai hình thức
                            </p>
                            <p class="coupon-text">
                                <i class="bi bi-credit-card-2-front"></i> GPLX (đối chiếu) & CCCD (đối chiếu VNeID)
                            </p>
                            <p class="coupon-text">
                                <i class="bi bi-passport-fill"></i> GPLX (đối chiếu) & Passport (giữ lại)
                            </p>
                        </div> 
                    </div>
                    <h3>Bạn có mã giảm giá? <span id="showcoupon">Ấn vào đây để nhập mã giảm giá</span></h3>
                    <div id="checkout_coupon" class="coupon-checkout-content" style="background-color:#FEF7F4; border-left: 8px solid #f26a2b;">
                        <div class="coupon-info">
                            <form action="#">
                                <p class="checkout-coupon">
                                    <input placeholder="Coupon code" type="text">
                                    <input class="coupon-inner_btn" value="Apply Coupon" type="submit">
                                </p>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-6 col-12 col-custom">
                <form id="checkoutForm" >
                    <div class="checkbox-form">
                        <h3>Chi tiết </h3>
                        <div class="row">
                            <div class="different-address">
                                <div class="order-notes mt-3">
                                    <div class="checkout-form-list checkout-form-list-2">
                                        <label>Lời nhắn</label>
                                        <textarea  id="notes" cols="30" rows="10" placeholder="Lời nhắn về thời gian, địa điểm giao xe"></textarea>
                                    </div>
                                </div>
                                <div class="ship-different-title">
                                    <div>
                                        <input id="ship-box" type="checkbox">
                                        <label for="ship-box">Thanh toán bằng VnPay</label>
                                    </div>
                                </div>
                            </div>
                          @*   <div class="col-md-6 col-custom">
                                <div class="checkout-form-list">
                                    <label>Lời nhắn <span class="required">*</span></label>
                                    <textarea id="notes" placeholder="Nhập lời nhắn" ></textarea>
                                </div>
                            </div> *@
                            <button type="submit" class="btn flosun-button secondary-btn black-color rounded-0 w-100 mt-4">Thanh toán</button>
                            @* <button type="submit" name="paymentmethod" class="btn flosun-button secondary-btn black-color rounded-0 w-100" value="Thanh toán COD" /><button>
                            <input type="submit" name="paymentmethod" class="btn flosun-button secondary-btn black-color rounded-0 w-100 mt-2" value="Thanh toán VNPay" />
                             *@



                        </div>

                    </div>
                </form>
            </div>
            <div class="col-lg-6 col-12 col-custom">
                <div class="your-order">
                    <h3>Bảng tính giá</h3>
                    <div class="your-order-table table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th class="cart-product-name">Nội dung</th>
                                    <th class="cart-product-total">Tổng</th>
                                </tr>
                            </thead>
                            <tbody>
                                
                                    <tr class="cart_item">
                                        <td class="cart-product-name">
                                            Thành tiền<strong class="product-quantity">
                                                
                                            </strong>
                                        </td>
                                        <td class="cart-product-total text-center"><span class="amount">@Model.Total đ</span></td>
                                    </tr>

                                

                            </tbody>
                            <tfoot>
                                <tr class="cart-subtotal">
                                    <th>Thanh toán giữ chỗ</th>
                                    <td class="text-center"><span class="amount">@Model.Deposit đ</span></td>
                                </tr>
                                <tr class="order-total">
                                    <th>Thanh toán khi nhận xe</th>
                                    <td class="text-center"><strong><span class="amount">@Model.Payment</span></strong></td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                    @* <div class="payment-method">
                        <div class="payment-accordion">
                            <div id="accordion">
                                <div class="card">
                                    <div class="card-header" id="#payment-1">
                                        <h5 class="panel-title mb-3">
                                            <a href="#" class="" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                                                Direct Bank Transfer.
                                            </a>
                                        </h5>
                                    </div>
                                    <div id="collapseOne" class="collapse show" data-parent="#accordion">
                                        <div class="card-body mb-2 mt-2">
                                            <p>Make your payment directly into our bank account. Please use your Order ID as the payment reference. Your order won’t be shipped until the funds have cleared in our account.</p>
                                        </div>
                                    </div>
                                </div>
                                <div class="card">
                                    <div class="card-header" id="#payment-2">
                                        <h5 class="panel-title mb-3">
                                            <a href="#" class="collapsed" data-bs-toggle="collapse" data-bs-target="#collapseTwo" aria-expanded="false" aria-controls="collapseTwo">
                                                Cheque Payment
                                            </a>
                                        </h5>
                                    </div>
                                    <div id="collapseTwo" class="collapse" data-parent="#accordion">
                                        <div class="card-body mb-2 mt-2">
                                            <p>Make your payment directly into our bank account. Please use your Order ID as the payment reference. Your order won’t be shipped until the funds have cleared in our account.</p>
                                        </div>
                                    </div>
                                </div>
                                <div class="card">
                                    <div class="card-header" id="#payment-3">
                                        <h5 class="panel-title mb-3">
                                            <a href="#" class="collapsed" data-bs-toggle="collapse" data-bs-target="#collapseThree" aria-expanded="false" aria-controls="collapseThree">
                                                PayPal
                                            </a>
                                        </h5>
                                    </div>
                                    <div id="collapseThree" class="collapse" data-parent="#accordion">
                                        <div class="card-body mb-2 mt-2">
                                            <p>Make your payment directly into our bank account. Please use your Order ID as the payment reference. Your order won’t be shipped until the funds have cleared in our account.</p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="order-button-payment">
                                <button class="btn flosun-button secondary-btn black-color rounded-0 w-100">Place Order</button>
                            </div>
                        </div>
                    </div> *@
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Checkout Area End Here -->
@section scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        $(document).ready(function () {
            $("#checkoutForm").submit(function (e) {
                e.preventDefault(); // Ngăn chặn submit form mặc định

                // Lấy giá trị từ checkbox mỗi lần submit
                var paymentMethod = $("#ship-box").is(":checked") ? "Thanh toán VNPay" : "Thanh toán COD";

                var formData = {
                    Notes: $("#notes").val(),
                    paymentmethod: paymentMethod // Thêm giá trị paymentMethod vào dữ liệu gửi
                };

                $.ajax({
                    url: '/Cart/Checkout', // Đường dẫn đến action
                    type: 'POST',
                    contentType: 'application/json',
                    data: JSON.stringify(formData),
                    success: function (response) {
                        if (response.success) {
                            // Nếu là thanh toán VNPay thì chuyển thẳng sang trang thanh toán của VNPay
                            if (response.isVnpay) {
                                window.location.href = response.redirectUrl;
                            } else {
                                // Hiển thị dialog đặt xe thành công, sau đó chuyển đến trang đơn hàng
                                Swal.fire({
                                    icon: 'success',
                                    title: 'Đặt xe thành công!',
                                    text: 'Bạn có thể xem chi tiết trong mục Đơn hàng của bạn.',
                                    confirmButtonText: 'Xem đơn hàng',
                                    confirmButtonColor: '#667eea',
                                    allowOutsideClick: false,
                                    allowEscapeKey: false
                                }).then(() => {
                                    window.location.href = response.redirectUrl;
                                });
                            }
                        } else {
                            // Kiểm tra nếu cần cập nhật địa chỉ
                            if (response.needAddress) {
                                // Hiển thị dialog với SweetAlert2 - chỉ có 1 nút OK
                                Swal.fire({
                                    icon: 'warning',
                                    title: 'Chưa cập nhật địa chỉ',
                                    text: response.message,
                                    confirmButtonText: 'Cập nhật địa chỉ ngay',
                                    confirmButtonColor: '#667eea',
                                    allowOutsideClick: false,
                                    allowEscapeKey: false
                                }).then((result) => {
                                    // Khi bấm OK thì chuyển đến trang cập nhật địa chỉ
                                    if (result.isConfirmed) {
                                        window.location.href = response.redirectUrl;
                                    } else {
                                        // Nếu đóng bằng cách khác thì cũng chuyển
                                        window.location.href = response.redirectUrl;
                                    }
                                });
                            } 
                            // Kiểm tra nếu cần cập nhật GPLX
                            else if (response.needLicense) {
                                // Hiển thị dialog với SweetAlert2 - chỉ có 1 nút OK
                                Swal.fire({
                                    icon: 'warning',
                                    title: 'Chưa cập nhật GPLX',
                                    text: response.message,
                                    confirmButtonText: 'Cập nhật GPLX ngay',
                                    confirmButtonColor: '#667eea',
                                    allowOutsideClick: false,
                                    allowEscapeKey: false
                                }).then((result) => {
                                    // Khi bấm OK thì chuyển đến trang My Account với tab GPLX
                                    if (result.isConfirmed) {
                                        window.location.href = response.redirectUrl;
                                    } else {
                                        // Nếu đóng bằng cách khác thì cũng chuyển
                                        window.location.href = response.redirectUrl;
                                    }
                                });
                            } 
                            else {
                                // Hiển thị thông báo lỗi thông thường
                                Swal.fire({
                                    icon: 'error',
                                    title: 'Lỗi',
                                    text: response.message,
                                    confirmButtonColor: '#667eea'
                                });
                            }
                        }
                    },
                    error: function () {
                        Swal.fire({
                            icon: 'error',
                            title: 'Lỗi',
                            text: 'Có lỗi xảy ra. Vui lòng thử lại.',
                            confirmButtonColor: '#667eea'
                        });
                    }
                });
            });
        });
    </script>


    </script>

}